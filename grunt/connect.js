module.exports = {
    webapp: {
        options: {
            port: 8080,
            hostname: 'localhost'
        }
    },

    dist: {
        options: {
            port: 8090,
            hostname: 'localhost'
        }
    }
};